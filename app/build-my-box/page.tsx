// Stripped-down entry point for WhatsApp ad traffic — same builder as
// app/build/page.tsx, just without the marketing header (see the `compact`
// check there). One copy of the wizard logic, two ways in.
export { default } from "../build/page";
